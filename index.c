#include <stdio.h> // Inclui a biblioteca de entrada/saída padrão em C

int main()
{                              // Declaração da função principal "main", que retorna um valor inteiro (int)
    printf("Hello, world!\n"); // Imprime a mensagem "Hello, world!" seguida por uma nova linha (\n)
    return 0;                  // Retorna 0 para indicar que o programa foi concluído com sucesso
} // Fim da função principal
